{% do attrs.set(id="navMobile", class="cd-nav-mobile") %}

<Popover :__attrs="attrs">
  <header>
    <Logo :base_url="nav.base_url" class="logo" />
    <nav class="cd-navbar nav-links">
      <ThemeSwitch class="themeswitch" :text="false" />
    </nav>
    <PopButton
      target="navMobile"
      action="close"
      title="Close menu"
      class="cd-toggle-sidebar"
    >Close</PopButton>
  </header>

  <Toc class="toc" :toc="nav.toc" :page="nav.page" />
</Popover>
