{#def title="", description="" #}

{% set DEFAULT_TITLE = "Documentation" %}
{% set title = (title + " | " + DEFAULT_TITLE) if title else DEFAULT_TITLE %}

<!DOCTYPE html>
<html lang="{{ nav.page.lang }}" class="light font-sans">
	<head>
		<meta charset="utf-8">
		<title>{{ title }}</title>
		<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
		<link rel="icon" type="image/x-icon" href="/static/favicon.ico">

		<link rel="stylesheet" href="/static/docs.css?v={{utils.timestamp}}">
		<script src="/static/docs.js?v={{utils.timestamp}}"></script>
		{{ catalog.render_assets() }}

		<meta property="og:title" content="{{ title }}">
		<meta property="generator" content="claydocs" />
		{% if description -%}<meta property="og:description" content="{{ description }}">{% endif -%}
		<meta property="og:type" content="profile" />
	</head>
	<body class="
		antialiased
		overflow-x-hidden overflow-y-auto
		text-default-color
		bg-default-color
		accent-accent dark:accent-accent-darker
	">
		{{ content }}
		{% Footer end %}Footer %}
		{% SearchDialog id="search-dialog" end %}SearchDialog %}
	</body>
</html>
